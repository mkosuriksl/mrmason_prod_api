# Active profile is supplied externally by the deployment environment.
# DEV  -> SPRING_PROFILES_ACTIVE=dev
# PROD -> SPRING_PROFILES_ACTIVE=prod
# Default profile used when SPRING_PROFILES_ACTIVE is not set externally.

spring.profiles.active=dev
